'use strict';

//The project will be only using low level driver for my sql.
//ORM can also be implemented for scalbilty purpose

//The config of database and table
var config = { 
    host: 'localhost',
    user: 'eg',
    password: 'Qwerty@12',
    database: 'booking'
}

module.exports = config;
